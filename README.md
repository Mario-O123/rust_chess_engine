
test example:
listed command to run the MVP in the terminal
# default (debug)
cargo run --bin terminal_proto_classical
![Run of debug-classical evaluator](images/classical/run_debug_classical.jpg)



# faster (release)
cargo run --release --bin terminal_proto_classical
